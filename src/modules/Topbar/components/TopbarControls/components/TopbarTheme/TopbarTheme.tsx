import { ActionButton } from '@/shared/ui/ActionButton'
import { SunIcon } from 'lucide-react'

const TopbarTheme = () => {
  return (
    <ActionButton>
      <SunIcon size={20} />
    </ActionButton>
  )
}

export { TopbarTheme }
